# Example connection strings for different environments
# Copy this file to .env and fill in your actual values

# Development Database
DEV_DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=localhost;Database=EnergyCertificate_Dev;UID=dev_user;PWD=dev_password"

# Staging Database  
STAGING_DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=staging-server;Database=EnergyCertificate_Staging;UID=staging_user;PWD=staging_password"

# Production Database
PROD_DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=prod-server;Database=EnergyCertificate;UID=prod_user;PWD=prod_password"

# Generic fallback (used if environment-specific not found)
DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=your_server;Database=EnergyCertificate;UID=your_user;PWD=your_password"

# Alternative connection string formats:

# Windows Authentication
# DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=your_server;Database=EnergyCertificate;Trusted_Connection=yes"

# Azure SQL Database
# DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=your_server.database.windows.net;Database=EnergyCertificate;UID=your_user@your_server;PWD=your_password;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30"

# SQL Server Express LocalDB
# DATABASE_CONNECTION_STRING="Driver={ODBC Driver 17 for SQL Server};Server=(localdb)\\MSSQLLocalDB;Database=EnergyCertificate;Trusted_Connection=yes"
